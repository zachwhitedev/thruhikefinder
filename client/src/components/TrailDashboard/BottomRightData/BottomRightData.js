import React from 'react';
import styles from './BottomRightData.module.css';

export default function BottomRightData(props) {
  return (
    <div className={styles.bottomRightDataContainer}>
      <div className={styles.contentContainer}>
        <p>
          Would be a chore and something we do later, but like I said, very easy
          to replicate this map in photoshop and also use a couple guidebooks to
          make one for each of the major thru hikes, if we decide it is worth
          taking the time to do it.
        </p>
        <p>
          Would be a chore and something we do later, but like I said, very easy
          to replicate this map in photoshop and also use a couple guidebooks to
          make one for each of the major thru hikes, if we decide it is worth
          taking the time to do it.
        </p>
      </div>
    </div>
  );
}
